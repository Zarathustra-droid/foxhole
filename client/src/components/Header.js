import React from 'react';
import SearchBar from './SearchBar';

const Header = () => {
  return (
    <div class="main-header">
      <h3 id="banner">Welcome to the Foxhole!</h3>
      <SearchBar />
    </div>
  );
};

export default Header;
